import { buttonVariants } from '~/styles/component-styles';

export const magicButtonStyles = {
    genie: buttonVariants.primary,
    primary: buttonVariants.primary,
    caramel: buttonVariants.chip,
    success: buttonVariants.sidebar,
}